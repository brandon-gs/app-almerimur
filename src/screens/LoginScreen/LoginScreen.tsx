import { Message, SafeAreaView } from "components/";
import { LoginContainer } from "containers/";
import React from "react";

function LoginScreen() {
  return (
    <SafeAreaView>
      <LoginContainer />
    </SafeAreaView>
  );
}

export default LoginScreen;
